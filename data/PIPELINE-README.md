# VDJ Supervised Pipeline — Documentation

## Overview

This pipeline automates the VirtualDJ → VideoFiles.json → Website workflow with a **human approval gate**. Nothing goes live until you explicitly approve it.

## Architecture