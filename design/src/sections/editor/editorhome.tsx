/**
 * Editor route — placeholder so router resolves.
 */
export default function EditorHome() {
  return (
    <div className="min-h-screen flex items-center justify-center p-4" style={{ background: "var(--rv-color-navy)", color: "var(--rv-color-butter)" }}>
      <div>
        <h1 className="text-xl font-semibold mb-2">Editor</h1>
        <p className="text-sm opacity-80">Match review / editor UI.</p>
      </div>
    </div>
  );
}
